name: "units-test"
on:
  pull_request:
  push:
    branches:
      - main
      - "releases/*"

jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci
      - run: npm test

  # test action works running from the graph
  test:
    runs-on: ubuntu-latest

    env:
      ZENDESK_SUBDOMAIN: "sejadigitalf21648233634"
      ZENDESK_API_TOKEN: "Qo4KjW6PAjMWNHw9q2gjjyI9JcJTF9ImY9a4Dvbg"
      ZENDESK_EMAIL: "maria.pereira@eteg.com.br"

    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          milliseconds: 1000
          env: "prod"
          path: "dist"
